# --- Runtime ---
ENVIRONMENT=production

# CORS: comma-separated origins. Use * to allow all (credentials will be disabled automatically)
ALLOWED_ORIGINS=https://your-frontend.example
# ALLOWED_ORIGINS=*

# --- Database (MongoDB) ---
MONGO_URI=mongodb+srv://<user>:<pass>@<cluster-host>/?retryWrites=true&w=majority
MONGO_DB_NAME=AI-lead-recovery

# Email-reply agent collection name
MONGODB_CAMPAIGN_COLLECTION=campaigns

# --- Auth / JWT ---
JWT_SECRET_KEY=change-me-to-a-long-random-string
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# --- OpenAI ---
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# --- Booking + Emails ---
BOOKING_BASE_URL=https://booking.example.com
EMAIL_FROM_ADDRESS=noreply@clinic.example.com

# --- Gmail / Google (enable only if using Gmail watch + Pub/Sub) ---
GOOGLE_CLIENT_SECRETS_FILE=/secure/google/confidential.json
GOOGLE_TOKEN_FILE=/secure/google/token.json
GMAIL_USER_EMAIL=you@example.com
GMAIL_TOPIC_NAME=projects/your-project/topics/your-topic
GMAIL_LABEL_IDS=INBOX
GMAIL_LABEL_FILTER_ACTION=include
GMAIL_PROCESS_REPLIES_ONLY=false

# Pub/Sub push security
PUBSUB_VERIFICATION_TOKEN=some-random-secret
# PUBSUB_OIDC_AUDIENCE=// optional, e.g. audience of your push endpoint

# --- SMTP (used by agent email sender; optional if only using Gmail API send) ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-smtp-user@example.com
SMTP_PASSWORD=your-smtp-password
SMTP_FROM_EMAIL=${SMTP_USERNAME}
SMTP_FROM_NAME=Clinic


